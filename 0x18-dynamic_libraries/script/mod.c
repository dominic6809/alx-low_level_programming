#include <stdio.h>

/**
 * mod - function that computes the modulus two integers
 * @a: first integer input
 * @b: second integer input
 * Return: 0 success, -1 error
 */
int mod(int a, int b)
{
	return (a % b);
}
