#include <stdio.h>

/**
 * sub - program that substracts two integers
 * @a: first integer input
 * @b: second integer input
 * Return: 0 success, -1 error
 */
int sub(int a, int b)
{
	return (a - b);
}
